from plots.graph import Graph

if __name__ == "__main__":

    # Create an instance of the Graph class and call the plotting methods
    graph = Graph()
    # graph.presence_by_date()
    # graph.average_by_day_of_week()
    #graph.correlation_matrix()
    # graph.correlation_matrix_without_departments()
    #graph.correlation_global_reservation()
    # graph.boxplot_by_day_of_week()
    # graph.heatmap_month_by_day()
    #graph.show_days_where_autre_is_one()
    graph.correlation_global_temp()
    graph.correlation_global_pluie()